import Link from 'next/link';
import React from 'react';
import { FC } from 'react';

export const Header: FC = () => {
    return (
        <header>
            <Link href="#" />
        </header>
    );
};

